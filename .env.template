# --- LLM Configuration ---
# 指定使用的 LLM 服务: 'gemini' 或 'deepseek'
LLM_PROVIDER=deepseek

# Google Gemini API Key (如果 LLM_PROVIDER=gemini 或 EMBEDDING_PROVIDER=google 则必需)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# DeepSeek API Key (如果 LLM_PROVIDER=deepseek 则必需)
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_API_KEY_HERE

# --- Embedding Configuration ---
# 指定使用的 Embedding 服务: 'google' 或 'ollama'
EMBEDDING_PROVIDER=ollama

# 如果 EMBEDDING_PROVIDER=ollama
# Docker Compose 中 Ollama 服务的地址和端口
OLLAMA_BASE_URL=http://ollama:11434
# 使用的 Ollama Embedding 模型名称
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# 如果 EMBEDDING_PROVIDER=google
# 使用的 Google Embedding 模型名称
GOOGLE_EMBEDDING_MODEL=models/embedding-001

# --- Optional: ScrapeGraphAI Settings ---
# 是否打印详细日志 (True/False)
# SCRAPEGRAPHAI_VERBOSE=True
# 是否使用无头浏览器模式 (True/False)
# SCRAPEGRAPHAI_HEADLESS=True
